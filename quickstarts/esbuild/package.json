{
  "name": "esbuild-quickstart",
  "scripts": {
    "build": "esbuild --bundle --outfile=build/bundle.js --sourcemap src/index.js",
    "build:min": "esbuild --bundle --minify --outfile=build/bundle.min.js --sourcemap src/index.js"
  },
  "dependencies": {
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "esbuild": "^0.12.19"
  }
}
